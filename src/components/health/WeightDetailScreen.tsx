import { router } from "expo-router";
import { useMemo, useState } from "react";
import { Pressable, StyleSheet, Text, useWindowDimensions, View } from "react-native";
import Svg, { Circle, Line, Polyline } from "react-native-svg";

import { GeneralHealthActivityDetailSheet } from "@/components/health/GeneralHealthActivityDetailSheet";
import { useGeneralHealthActivity } from "@/components/health/GeneralHealthActivityProvider";
import { HealthScreenContainer } from "@/components/health/HealthScreenContainer";
import { GeneralHealthLogSheet } from "@/components/health/GeneralHealthLogSheet";
import { AppButton, AppCard, AppIcon, AppSection } from "@/components/ui";
import { createGeneralHealthActivity } from "@/lib/generalHealthActivity";
import type { GeneralHealthActivityEntry, GeneralHealthLogDraft } from "@/lib/generalHealthMockData";
import { useAppTheme } from "@/theme/ThemeProvider";

type WeightRange = 7 | 30 | 90 | "all";
type WeightUnit = "kg" | "lb";
type WeightPoint = { activity: Extract<GeneralHealthActivityEntry, { type: "weight" }>; id: string; recordedAt: string; valueKilograms: number };
type WeightSummary = { averageKg: number; changeKg: number; count: number; highestKg: number; lowestKg: number };
const RANGE_OPTIONS: { label: string; value: WeightRange }[] = [{ label: "7D", value: 7 }, { label: "30D", value: 30 }, { label: "90D", value: 90 }, { label: "All", value: "all" }];
const SCREEN_STATE: "error" | "loading" | "ready" = "ready";

export function WeightDetailScreen() {
  const { theme } = useAppTheme();
  const { activities, addActivity, selectedProfileId, selectedProfileName } = useGeneralHealthActivity();
  const [unit, setUnit] = useState<WeightUnit>("kg");
  const [range, setRange] = useState<WeightRange>(30);
  const [selectedId, setSelectedId] = useState<string | null>(null);
  const [detailActivity, setDetailActivity] = useState<GeneralHealthActivityEntry | null>(null);
  const [logOpen, setLogOpen] = useState(false);
  const [openedAt] = useState(() => new Date());
  // TODO: Replace feature-local weight entries with the approved profile health data layer and supported connected-scale sources.
  // TODO: Read and persist weight display units through the selected profile's country and unit settings.
  // TODO: Let the shared Add Weight sheet use the selected profile's preferred entry unit while preserving canonical kilograms.
  // TODO: Normalize legacy weight activities only when their stored unit can be identified without guessing.
  const allPoints = useMemo(() => getWeightEntriesForProfile(activities, selectedProfileId), [activities, selectedProfileId]);
  const points = useMemo(() => filterWeightEntriesByRange(allPoints, range, openedAt), [allPoints, openedAt, range]);
  const summary = useMemo(() => calculateWeightSummary(points), [points]);
  const selectedPoint = points.find((point) => point.id === selectedId) ?? points.at(-1) ?? null;
  function saveWeight(draft: GeneralHealthLogDraft) { const activity = createGeneralHealthActivity(draft, selectedProfileId, selectedProfileName); addActivity(activity); setSelectedId(activity.id); setLogOpen(false); }
  return <View style={[styles.screen, { backgroundColor: theme.background }]}><HealthScreenContainer bottomSpacing={96} contentStyle={styles.content}><MetricHeader profileName={selectedProfileName} />{SCREEN_STATE === "loading" ? <LoadingState /> : null}{SCREEN_STATE === "error" ? <ScreenState message="Please return to General Health and open this screen again." title="We couldn't load weight history" /> : null}{SCREEN_STATE === "ready" && !allPoints.length ? <ScreenState action="Add weight entry" message="Add an entry to begin building your personal history." onAction={() => setLogOpen(true)} title="No weight entries yet" /> : null}{SCREEN_STATE === "ready" && allPoints.length ? <><LatestCard points={allPoints} unit={unit} /><UnitSelector setUnit={setUnit} unit={unit} /><RangeSelector range={range} setRange={(next) => { setRange(next); setSelectedId(null); }} />{!points.length ? <ScreenState action="Show all" message="Choose another range or add a new entry." onAction={() => { setRange("all"); setSelectedId(null); }} title="No entries in this time range" /> : <><WeightChart onSelect={setSelectedId} points={points} range={range} selectedId={selectedPoint?.id ?? null} summary={summary} unit={unit} />{selectedPoint ? <SelectedEntry point={selectedPoint} unit={unit} /> : null}<SavedSummary summary={summary} unit={unit} /><RecentEntries entries={[...points].reverse().slice(0, 5)} onOpen={setDetailActivity} unit={unit} /></>}<AppButton accessibilityLabel="Add weight entry" fullWidth label="Add weight entry" onPress={() => setLogOpen(true)} /><NeutralNote /></> : null}</HealthScreenContainer><GeneralHealthActivityDetailSheet activity={detailActivity?.profileId === selectedProfileId ? detailActivity : null} onClose={() => setDetailActivity(null)} /><GeneralHealthLogSheet key={logOpen ? "weight-open" : "weight-closed"} logType={logOpen ? "weight" : null} onCancel={() => setLogOpen(false)} onSave={saveWeight} profileName={selectedProfileName} /></View>;
}

function MetricHeader({ profileName }: { profileName: string }) { const { theme } = useAppTheme(); return <View style={styles.header}><Pressable accessibilityHint="Returns to General Health" accessibilityLabel="Back to General Health" accessibilityRole="button" onPress={() => router.back()} style={({ pressed }) => [styles.back, { backgroundColor: theme.surface, borderColor: theme.border }, pressed ? styles.pressed : null]}><Text style={[styles.backText, { color: theme.text }]}>{"<"}</Text></Pressable><View style={styles.headerCopy}><Text accessibilityRole="header" style={[styles.title, { color: theme.text }]}>Weight</Text><Text style={[styles.subtitle, { color: theme.mutedText }]}>Saved entries and personal trends</Text></View><View accessibilityLabel={`Active profile, ${profileName}`} accessible style={styles.profile}><View style={[styles.profileIcon, { backgroundColor: theme.primarySoft }]}><AppIcon color={theme.primary} decorative name="profile" size={16} /></View><Text numberOfLines={1} style={[styles.profileName, { color: theme.mutedText }]}>{profileName}</Text></View></View>; }
function LatestCard({ points, unit }: { points: WeightPoint[]; unit: WeightUnit }) { const { theme } = useAppTheme(); const latest = points.at(-1)!; const previous = points.at(-2), changeKg = previous ? latest.valueKilograms - previous.valueKilograms : null, displayChange = changeKg === null ? null : convertKilograms(changeKg, unit); const comparison = displayChange === null ? "Add more entries to see changes over time." : Math.abs(changeKg!) < 0.05 ? "No change from your previous saved entry." : `${formatNumber(Math.abs(displayChange))} ${unit} ${displayChange > 0 ? "higher" : "lower"} than your previous saved entry.`; return <AppCard style={[styles.hero, { backgroundColor: theme.primarySoft, borderColor: theme.primary }]}><View pointerEvents="none" style={styles.heroLines}><View style={[styles.heroLine, { backgroundColor: theme.primary }]} /><View style={[styles.heroLine, styles.heroLineTwo, { backgroundColor: theme.secondary }]} /></View><Text style={[styles.eyebrow, { color: theme.primary }]}>LATEST ENTRY</Text><Text accessibilityLabel={`Latest weight entry, ${formatWeightSpeech(latest.valueKilograms, unit)}, saved ${formatDateTime(latest.recordedAt)}. ${comparison}`} adjustsFontSizeToFit numberOfLines={1} style={[styles.latestValue, { color: theme.text }]}>{formatNumber(convertKilograms(latest.valueKilograms, unit))}<Text style={styles.latestUnit}> {unit}</Text></Text><Text style={[styles.heroTime, { color: theme.mutedText }]}>Saved {formatDateTime(latest.recordedAt)}</Text><Text style={[styles.heroSupport, { color: theme.mutedText }]}>{comparison}</Text></AppCard>; }
function UnitSelector({ setUnit, unit }: { setUnit: (unit: WeightUnit) => void; unit: WeightUnit }) { const { theme } = useAppTheme(); return <View style={styles.selectorBlock}><Text style={[styles.selectorLabel, { color: theme.mutedText }]}>Display</Text><View style={styles.unitRow}>{(["kg", "lb"] as const).map((option) => { const active = option === unit; return <Pressable accessibilityLabel={`${option === "kg" ? "Kilograms" : "Pounds"}, ${active ? "selected" : "not selected"}`} accessibilityRole="button" accessibilityState={{ selected: active }} key={option} onPress={() => setUnit(option)} style={({ pressed }) => [styles.unitButton, { backgroundColor: active ? theme.primarySoft : theme.surface, borderColor: active ? theme.primary : theme.border }, pressed ? styles.pressed : null]}><Text style={[styles.selectorText, { color: active ? theme.primary : theme.text }]}>{active ? "Selected " : ""}{option}</Text></Pressable>; })}</View></View>; }
function RangeSelector({ range, setRange }: { range: WeightRange; setRange: (range: WeightRange) => void }) { const { theme } = useAppTheme(); return <View style={styles.selectorBlock}><Text style={[styles.selectorLabel, { color: theme.mutedText }]}>Time range</Text><View style={styles.rangeRow}>{RANGE_OPTIONS.map((option) => { const active = range === option.value; return <Pressable accessibilityLabel={`${option.value === "all" ? "All time" : `${option.value} days`}, ${active ? "selected" : "not selected"}`} accessibilityRole="button" accessibilityState={{ selected: active }} key={option.label} onPress={() => setRange(option.value)} style={({ pressed }) => [styles.rangeButton, { backgroundColor: active ? theme.primarySoft : theme.surface, borderColor: active ? theme.primary : theme.border }, pressed ? styles.pressed : null]}><Text style={[styles.selectorText, { color: active ? theme.primary : theme.text }]}>{active ? "Selected " : ""}{option.label}</Text></Pressable>; })}</View></View>; }

function WeightChart({ onSelect, points, range, selectedId, summary, unit }: { onSelect: (id: string) => void; points: WeightPoint[]; range: WeightRange; selectedId: string | null; summary: WeightSummary; unit: WeightUnit }) { const { theme } = useAppTheme(); const { width: screenWidth } = useWindowDimensions(); const width = Math.max(160, Math.min(screenWidth - 70, 410)), height = 210, plot = getChartPlot(points, width, height), labels = getXAxisLabels(plot.positions), selectedPoint = plot.positions.find((point) => point.id === selectedId) ?? null; const accessibilityLabel = `Weight trend for ${range === "all" ? "all saved time" : `the last ${range} days`}. ${summary.count} saved entries. Lowest saved weight ${formatWeightSpeech(summary.lowestKg, unit)}, highest ${formatWeightSpeech(summary.highestKg, unit)}, average ${formatWeightSpeech(summary.averageKg, unit)}.`; function selectNearest(locationX: number) { onSelect(plot.positions.reduce((current, point) => Math.abs(point.x - locationX) < Math.abs(current.x - locationX) ? point : current).id); } return <AppCard style={[styles.chartCard, { borderColor: theme.border }]}><View style={styles.chartHeading}><Text style={[styles.sectionTitle, { color: theme.text }]}>Weight trend</Text><Text style={[styles.chartUnit, { color: theme.mutedText }]}>{unit}</Text></View><Text accessibilityLabel={accessibilityLabel} accessible style={styles.srSummary}>{accessibilityLabel}</Text><View onTouchEnd={(event) => selectNearest(event.nativeEvent.locationX)} style={[styles.chart, { height, width }]}><Svg height={height} pointerEvents="none" width={width}>{plot.ticks.map((tick) => <Line key={tick.valueKg} stroke={theme.border} strokeWidth="1" x1={plot.left} x2={width - plot.right} y1={tick.y} y2={tick.y} />)}{selectedPoint ? <Line stroke={theme.mutedText} strokeDasharray="3 4" strokeWidth="1" x1={selectedPoint.x} x2={selectedPoint.x} y1={plot.top} y2={height - plot.bottom} /> : null}{plot.positions.length > 1 ? <Polyline fill="none" points={plot.positions.map((point) => `${point.x},${point.y}`).join(" ")} stroke={theme.primary} strokeWidth="2.5" /> : null}{plot.positions.map((point) => <Circle cx={point.x} cy={point.y} fill={point.id === selectedId ? theme.surface : theme.primary} key={point.id} r={point.id === selectedId ? 7 : 4} stroke={theme.primary} strokeWidth={point.id === selectedId ? 3 : 1} />)}</Svg>{plot.ticks.map((tick) => <Text key={tick.valueKg} pointerEvents="none" style={[styles.yLabel, { color: theme.mutedText, top: tick.y - 8 }]}>{formatNumber(convertKilograms(tick.valueKg, unit))}</Text>)}{selectedPoint ? <View pointerEvents="none" style={[styles.chartTooltip, { backgroundColor: theme.text, left: clamp(selectedPoint.x - 42, plot.left, width - plot.right - 84), top: clamp(selectedPoint.y - 43, 2, height - 38) }]}><Text style={[styles.chartTooltipText, { color: theme.background }]}>{formatWeight(selectedPoint.valueKilograms, unit)}</Text></View> : null}{plot.positions.map((point) => <Pressable accessibilityLabel={`${formatWeightSpeech(point.valueKilograms, unit)}, ${formatFullDateTime(point.recordedAt)}, added manually`} accessibilityRole="button" accessibilityState={{ selected: point.id === selectedId }} key={point.id} onPress={() => onSelect(point.id)} style={[styles.pointTarget, { left: point.x - 22, top: point.y - 22 }]} />)}</View><View style={[styles.xLabels, { paddingLeft: plot.left, paddingRight: plot.right }]}>{labels.map((point) => <Text key={point.id} numberOfLines={1} style={[styles.axisText, { color: theme.mutedText }]}>{formatAxisDate(point.recordedAt, range)}</Text>)}</View>{points.length === 1 ? <Text style={[styles.singleText, { color: theme.mutedText }]}>Add more entries to see a trend.</Text> : null}</AppCard>; }
function SelectedEntry({ point, unit }: { point: WeightPoint; unit: WeightUnit }) { const { theme } = useAppTheme(); return <View accessibilityLabel={`Selected weight entry, ${formatWeightSpeech(point.valueKilograms, unit)}, ${formatFullDateTime(point.recordedAt)}, added manually.${point.activity.details.notes ? ` Note: ${point.activity.details.notes}` : ""}`} accessible style={[styles.selected, { backgroundColor: theme.surface, borderColor: theme.border, borderLeftColor: theme.primary }]}><Text style={[styles.selectedKicker, { color: theme.primary }]}>SELECTED ENTRY</Text><Text style={[styles.selectedValue, { color: theme.text }]}>{formatWeight(point.valueKilograms, unit)}</Text><Text style={[styles.selectedMeta, { color: theme.mutedText }]}>{formatFullDateTime(point.recordedAt)}</Text>{point.activity.details.notes ? <Text numberOfLines={2} style={[styles.selectedNote, { color: theme.mutedText }]}>Note: {point.activity.details.notes}</Text> : null}<Text style={[styles.source, { color: theme.primary }]}>Added manually</Text></View>; }
function SavedSummary({ summary, unit }: { summary: WeightSummary; unit: WeightUnit }) { const { theme } = useAppTheme(); const change = convertKilograms(summary.changeKg, unit); const metrics = [{ accessibilityLabel: `Lowest saved weight, ${formatWeightSpeech(summary.lowestKg, unit)}`, label: "Lowest saved", value: formatWeight(summary.lowestKg, unit) }, { accessibilityLabel: `Highest saved weight, ${formatWeightSpeech(summary.highestKg, unit)}`, label: "Highest saved", value: formatWeight(summary.highestKg, unit) }, { accessibilityLabel: `Average saved weight, ${formatWeightSpeech(summary.averageKg, unit)}`, label: "Average saved", value: formatWeight(summary.averageKg, unit) }, { accessibilityLabel: `${summary.count} saved ${summary.count === 1 ? "entry" : "entries"}`, label: "Entries", value: `${summary.count}` }, ...(summary.count > 1 ? [{ accessibilityLabel: `Change from first to latest visible entry, ${formatSignedWeightSpeech(change, unit)}`, label: "Change", value: `${change > 0 ? "+" : ""}${formatNumber(change)} ${unit}` }] : [])]; return <AppSection subtitle="Calculated from the entries visible in this time range." title="Your saved range"><View style={styles.summaryGrid}>{metrics.map((metric) => <View accessibilityLabel={metric.accessibilityLabel} accessible key={metric.label} style={[styles.summaryCard, { backgroundColor: theme.surface, borderColor: theme.border }]}><Text style={[styles.summaryLabel, { color: theme.mutedText }]}>{metric.label}</Text><Text style={[styles.summaryValue, { color: theme.text }]}>{metric.value}</Text></View>)}</View></AppSection>; }
function RecentEntries({ entries, onOpen, unit }: { entries: WeightPoint[]; onOpen: (entry: GeneralHealthActivityEntry) => void; unit: WeightUnit }) { const { theme } = useAppTheme(); return <AppSection actionAccessibilityHint="Opens General Health history" actionAccessibilityLabel="View all health history" actionLabel="View all" onActionPress={() => router.push("/health/general/history")} title="Recent entries"><View style={styles.readingStack}>{entries.map((point) => <Pressable accessibilityHint="Opens the complete saved activity details." accessibilityLabel={`${formatWeightSpeech(point.valueKilograms, unit)}, ${formatFullDateTime(point.recordedAt)}, added manually${point.activity.details.notes ? ", includes a note" : ""}`} accessibilityRole="button" key={point.id} onPress={() => onOpen(point.activity)} style={({ pressed }) => [styles.readingRow, { backgroundColor: theme.surface, borderColor: theme.border }, pressed ? styles.pressed : null]}><View style={styles.readingCopy}><Text style={[styles.readingValue, { color: theme.text }]}>{formatWeight(point.valueKilograms, unit)}</Text><Text style={[styles.readingMeta, { color: theme.mutedText }]}>{formatRecentDateTime(point.recordedAt)}</Text><Text style={[styles.readingSource, { color: theme.mutedText }]}>Added manually{point.activity.details.notes ? " | Has note" : ""}</Text></View><Text style={[styles.chevron, { color: theme.mutedText }]}>{">"}</Text></Pressable>)}</View></AppSection>; }
function NeutralNote() { const { theme } = useAppTheme(); return <View style={[styles.safety, { borderColor: theme.border }]}><AppIcon color={theme.primary} decorative name="weight" size={18} /><Text style={[styles.safetyText, { color: theme.mutedText }]}>This screen helps you organize the weight entries you choose to save. It does not judge body size or provide a medical interpretation. Contact a healthcare professional if you have concerns about unexpected body changes.</Text></View>; }
function LoadingState() { const { theme } = useAppTheme(); return <View style={styles.loading}>{[150, 92, 270, 190, 90].map((height) => <View key={height} style={[styles.skeleton, { backgroundColor: theme.surface, borderColor: theme.border, height }]} />)}</View>; }
function ScreenState({ action, message, onAction, title }: { action?: string; message: string; onAction?: () => void; title: string }) { const { theme } = useAppTheme(); return <AppCard style={[styles.empty, { borderColor: theme.border }]}><AppIcon color={theme.primary} container name="weight" size={24} /><Text style={[styles.emptyTitle, { color: theme.text }]}>{title}</Text><Text style={[styles.emptyMessage, { color: theme.mutedText }]}>{message}</Text>{action && onAction ? <AppButton label={action} onPress={onAction} /> : null}</AppCard>; }

function getWeightEntriesForProfile(entries: GeneralHealthActivityEntry[], profileId: string): WeightPoint[] { return entries.flatMap((entry) => { if (entry.profileId !== profileId || entry.type !== "weight" || !entry.details.weight) return []; const valueKilograms = Number(entry.details.weight), recordedTime = new Date(entry.createdAt).getTime(); return Number.isFinite(valueKilograms) && valueKilograms > 0 && Number.isFinite(recordedTime) ? [{ activity: entry, id: entry.id, recordedAt: entry.createdAt, valueKilograms }] : []; }).sort((left, right) => new Date(left.recordedAt).getTime() - new Date(right.recordedAt).getTime()); }
function filterWeightEntriesByRange(points: WeightPoint[], range: WeightRange, reference: Date) { if (range === "all") return points; const cutoff = reference.getTime() - range * 86400000; return points.filter((point) => new Date(point.recordedAt).getTime() >= cutoff); }
function calculateWeightSummary(points: WeightPoint[]): WeightSummary { const values = points.map((point) => point.valueKilograms); return { averageKg: values.reduce((sum, value) => sum + value, 0) / values.length, changeKg: values.at(-1)! - values[0], count: values.length, highestKg: Math.max(...values), lowestKg: Math.min(...values) }; }
function kilogramsToPounds(value: number) { return value * 2.2046226218; }
function convertKilograms(value: number, unit: WeightUnit) { return unit === "kg" ? value : kilogramsToPounds(value); }
function formatNumber(value: number) { return value.toFixed(1); }
function formatWeight(valueKg: number, unit: WeightUnit) { return `${formatNumber(convertKilograms(valueKg, unit))} ${unit}`; }
function formatWeightSpeech(valueKg: number, unit: WeightUnit) { return `${formatNumber(convertKilograms(valueKg, unit))} ${unit === "kg" ? "kilograms" : "pounds"}`; }
function formatSignedWeightSpeech(value: number, unit: WeightUnit) { const sign = value > 0 ? "plus " : value < 0 ? "minus " : ""; return `${sign}${formatNumber(Math.abs(value))} ${unit === "kg" ? "kilograms" : "pounds"}`; }
function getChartPlot(points: WeightPoint[], width: number, height: number) { const left = 48, right = 24, top = 30, bottom = 22, valuesKg = points.map((point) => point.valueKilograms), rawMinKg = Math.min(...valuesKg), rawMaxKg = Math.max(...valuesKg), dataSpanKg = Math.max(rawMaxKg - rawMinKg, 2), paddingKg = Math.max(0.5, dataSpanKg * 0.2), intervalKg = 1, minKg = Math.floor((rawMinKg - paddingKg) / intervalKg) * intervalKg, maxKg = Math.ceil((rawMaxKg + paddingKg) / intervalKg) * intervalKg, spanKg = Math.max(maxKg - minKg, intervalKg), firstTime = new Date(points[0].recordedAt).getTime(), lastTime = new Date(points.at(-1)!.recordedAt).getTime(), timeSpan = Math.max(lastTime - firstTime, 1); const positions = points.map((point) => ({ ...point, x: points.length === 1 ? left + (width - left - right) / 2 : left + (new Date(point.recordedAt).getTime() - firstTime) / timeSpan * (width - left - right), y: top + (maxKg - point.valueKilograms) / spanKg * (height - top - bottom) })); const ticks = [maxKg, (maxKg + minKg) / 2, minKg].map((valueKg) => ({ valueKg, y: top + (maxKg - valueKg) / spanKg * (height - top - bottom) })); return { bottom, left, positions, right, ticks, top }; }
function clamp(value: number, minimum: number, maximum: number) { return Math.min(Math.max(value, minimum), maximum); }
function getXAxisLabels<T extends WeightPoint>(points: T[]) { if (points.length <= 4) return points; const indices = [0, Math.round((points.length - 1) / 3), Math.round((points.length - 1) * 2 / 3), points.length - 1]; return indices.filter((index, position) => indices.indexOf(index) === position).map((index) => points[index]); }
function formatDateTime(value: string) { return new Date(value).toLocaleString(undefined, { day: "numeric", hour: "numeric", minute: "2-digit", month: "short" }); }
function formatFullDateTime(value: string) { return new Date(value).toLocaleString(undefined, { day: "numeric", hour: "numeric", minute: "2-digit", month: "long", year: "numeric" }); }
function formatAxisDate(value: string, range: WeightRange) { return new Date(value).toLocaleDateString(undefined, range === 7 ? { weekday: "short" } : range === "all" ? { month: "short", year: "2-digit" } : { day: "numeric", month: "short" }); }
function formatRecentDateTime(value: string) { const date = new Date(value), today = new Date(), yesterday = new Date(today); yesterday.setDate(today.getDate() - 1); const label = sameDay(date, today) ? "Today" : sameDay(date, yesterday) ? "Yesterday" : date.toLocaleDateString(undefined, { day: "numeric", month: "short" }); return `${label} | ${date.toLocaleTimeString(undefined, { hour: "numeric", minute: "2-digit" })}`; }
function sameDay(left: Date, right: Date) { return left.getFullYear() === right.getFullYear() && left.getMonth() === right.getMonth() && left.getDate() === right.getDate(); }

const styles = StyleSheet.create({
  axisText: { flexShrink: 1, fontSize: 11, fontWeight: "800", minWidth: 34, textAlign: "center" }, back: { alignItems: "center", borderRadius: 16, borderWidth: 1, height: 44, justifyContent: "center", width: 44 }, backText: { fontSize: 22, fontWeight: "900" }, chart: { alignSelf: "center", position: "relative" }, chartCard: { borderWidth: 1, gap: 12, overflow: "hidden", padding: 16 }, chartHeading: { alignItems: "center", flexDirection: "row", justifyContent: "space-between" }, chartTooltip: { alignItems: "center", borderRadius: 10, minWidth: 84, paddingHorizontal: 8, paddingVertical: 6, position: "absolute" }, chartTooltipText: { fontSize: 11, fontWeight: "900" }, chartUnit: { fontSize: 12, fontWeight: "900" }, chevron: { fontSize: 20, fontWeight: "900" },
  content: { gap: 22 }, empty: { alignItems: "center", borderWidth: 1, gap: 12, padding: 24 }, emptyMessage: { lineHeight: 21, textAlign: "center" }, emptyTitle: { fontSize: 20, fontWeight: "900", textAlign: "center" }, eyebrow: { fontSize: 11, fontWeight: "900" }, header: { alignItems: "center", flexDirection: "row", gap: 12 }, headerCopy: { flex: 1, minWidth: 0 }, hero: { borderRadius: 26, borderWidth: 1, gap: 7, overflow: "hidden", padding: 20 }, heroLine: { borderRadius: 2, height: 3, opacity: 0.1, position: "absolute", right: 0, top: 18, width: 150 }, heroLines: { height: 80, position: "absolute", right: -20, top: 12, transform: [{ rotate: "-8deg" }], width: 170 }, heroLineTwo: { right: 20, top: 52, width: 115 }, heroSupport: { fontSize: 13, lineHeight: 20, marginTop: 5, maxWidth: "82%" }, heroTime: { fontSize: 13 }, latestUnit: { fontSize: 24, fontWeight: "800" }, latestValue: { fontSize: 42, fontWeight: "900", lineHeight: 50 }, loading: { gap: 14 },
  pointTarget: { height: 44, position: "absolute", width: 44 }, pressed: { opacity: 0.74 }, profile: { alignItems: "center", flexShrink: 1, gap: 4, maxWidth: 72 }, profileIcon: { alignItems: "center", borderRadius: 12, height: 32, justifyContent: "center", width: 32 }, profileName: { fontSize: 11, fontWeight: "800", maxWidth: 72, textAlign: "center" }, rangeButton: { alignItems: "center", borderRadius: 16, borderWidth: 1, flex: 1, justifyContent: "center", minHeight: 44 }, rangeRow: { flexDirection: "row", gap: 8 }, readingCopy: { flex: 1, minWidth: 0 }, readingMeta: { fontSize: 12, lineHeight: 18, marginTop: 4 }, readingRow: { alignItems: "center", borderRadius: 18, borderWidth: 1, flexDirection: "row", gap: 10, minHeight: 82, padding: 14 }, readingSource: { fontSize: 12, lineHeight: 18 }, readingStack: { gap: 10 }, readingValue: { fontSize: 17, fontWeight: "900" },
  safety: { alignItems: "flex-start", borderLeftWidth: 3, flexDirection: "row", gap: 10, paddingHorizontal: 14 }, safetyText: { flex: 1, fontSize: 12, lineHeight: 19 }, screen: { flex: 1 }, sectionTitle: { fontSize: 17, fontWeight: "900" }, selected: { borderLeftWidth: 4, borderRadius: 18, borderWidth: 1, gap: 5, padding: 15 }, selectedKicker: { fontSize: 11, fontWeight: "900" }, selectedMeta: { fontSize: 13, lineHeight: 19 }, selectedNote: { fontSize: 13, lineHeight: 19, marginTop: 4 }, selectedValue: { fontSize: 23, fontWeight: "900" }, selectorBlock: { gap: 8 }, selectorLabel: { fontSize: 12, fontWeight: "800" }, selectorText: { fontSize: 12, fontWeight: "900" }, singleText: { fontSize: 13, textAlign: "center" }, skeleton: { borderRadius: 22, borderWidth: 1 }, source: { fontSize: 12, fontWeight: "900", marginTop: 3 }, srSummary: { height: 1, opacity: 0, position: "absolute", width: 1 }, subtitle: { fontSize: 13, lineHeight: 18, marginTop: 3 },
  summaryCard: { borderRadius: 18, borderWidth: 1, flexBasis: "46%", flexGrow: 1, minHeight: 88, minWidth: 145, padding: 14 }, summaryGrid: { flexDirection: "row", flexWrap: "wrap", gap: 10 }, summaryLabel: { fontSize: 12, fontWeight: "800" }, summaryValue: { fontSize: 18, fontWeight: "900", marginTop: 8 }, title: { fontSize: 25, fontWeight: "900" }, unitButton: { alignItems: "center", borderRadius: 16, borderWidth: 1, flex: 1, justifyContent: "center", minHeight: 44 }, unitRow: { flexDirection: "row", gap: 8, maxWidth: 250 }, xLabels: { flexDirection: "row", justifyContent: "space-between" }, yLabel: { fontSize: 10, fontWeight: "800", left: 0, position: "absolute", width: 38 }
});
